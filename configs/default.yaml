classes: ["Robbery", "Burglary", "Shoplifting", "Stealing", "Normal"]
sequence_length: 20        # frames por video
img_size: 128
batch_size: 8              # bajo por los 6GB de VRAM
epochs: 30
learning_rate: 0.0001
optimizer: adam
backend: torch             # keras backend